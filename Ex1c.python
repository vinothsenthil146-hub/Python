from itertools import combinations

lst = [1, -2, 3, 4, -1]

print("Positive combinations:")

for r in range(len(lst) + 1):
    for comb in combinations(lst, r):
        if all(num > 0 for num in comb):
            print(comb)
